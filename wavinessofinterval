#leetcode pblm- 3751
problem:
You are given two integers num1 and num2 representing an inclusive range [num1, num2].
The waviness of a number is defined as the total count of its peaks and valleys:
A digit is a peak if it is strictly greater than both of its immediate neighbors.
A digit is a valley if it is strictly less than both of its immediate neighbors.
The first and last digits of a number cannot be peaks or valleys.
Any number with fewer than 3 digits has a waviness of 0.
Return the total sum of waviness for all numbers in the range [num1, num2].

solution:
       def totalWaviness(self, num1, num2):
        total = 0
        #waviness = peaks + valleys    
        for i in range(num1, num2 + 1):
            num = str(i)
            #converting the number into string to access each digit
            for p in range(1, len(num) - 1):
                if ((num[p] > num[p-1] and num[p] > num[p+1]) or #peaks condition
                    (num[p] < num[p-1] and num[p] < num[p+1])):  #valleys condition
                    total += 1
        return total            
 
